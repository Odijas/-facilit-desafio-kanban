global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: facilit-kanban-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["backend:8080"]
    basic_auth:
      username: prometheus
      password_file: /run/secrets/metrics_password
